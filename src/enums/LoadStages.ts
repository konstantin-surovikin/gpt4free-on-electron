enum LoadStages {
  SEARCH_PORT = 'Search port',
  FOUND_PORT = 'Port found',
  CREATE_SERVER = 'Create server',
  CREATED_SERVER = 'Server created',
  LOAD_CONVERSATIONS = 'Load conversations',
  FINISHED = 'Finished',
}

export default LoadStages;
